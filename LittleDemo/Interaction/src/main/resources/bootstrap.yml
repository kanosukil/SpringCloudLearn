server:
  port: 8801
spring:
  application:
    name: InteractionWithFe
  cloud:
    nacos:
      server-addr: 127.0.0.1:8800
      config:
        group: Configuration_Group
        name: interaction-config
        file-extension: yml